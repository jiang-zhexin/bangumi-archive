[project]
name = "bangumi-archive"
version = "0.1.0"
description = "https://github.com/bangumi/Archive for parquet"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "huggingface-hub>=1.29.0",
  "polars>=1.44.1",
]

[dependency-groups]
dev = [
  "ruff>=0.16.4",
  "ty>=0.0.74",
]
